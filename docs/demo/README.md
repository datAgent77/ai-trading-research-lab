# Demo materials (optional)

Add screenshots or short screen recordings here for README or talks.

Suggested captures:

1. **Backtest** — Rich table output from `uv run python scripts/run_backtest.py donchian SPY --start 2020-01-01 --end 2024-12-31`.
2. **Telegram** — `/start` and `/status` replies (blur tokens and account ids).
3. **Architecture** — export Mermaid from `README.md` or `docs/ARCHITECTURE.md` as PNG.

Filenames: `backtest-sample.png`, `telegram-sample.png`, etc.
